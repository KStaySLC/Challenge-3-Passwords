# EMPLOYEES with access to sensitive data need randomly generated passwords that 
# industry complexity requirements for enhanced security. 

### GIVEN I need a new, secure password
WHEN open the webpage
THEN I am presented with a series of prompts for password criteria.
User interface is restricted to passwords between 8 and 128 characters. 

WHEN I attempt to create a non-complex password
THEN I confirm whether or not to include lowercase, uppercase, numeric, and/or special characters
WHEN I answer each prompt
THEN my input should be validated and at least one character type should be selected
WHEN all prompts are answered
THEN a password is generated that matches the selected criteria
WHEN the password is generated
THEN the password is written to the page